<div class="row">
  <div class="col-md-6">
    <h1><%= fa_icon 'graduation-cap' %> Novo Minicurso</h1>
  </div>
  <div class="col-md-6">
    <div class="btn-group pull-right">
      <a href="<%= admin_minicursos_path %>" class="btn btn-primary"><%= fa_icon 'undo' %> Voltar</a>
    </div>
  </div>
</div>

<%= bootstrap_form_for(@minicurso, url: admin_minicursos_path(@minicurso)) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>
